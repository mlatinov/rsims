% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_gps.R
\name{simulate_gp_1d}
\alias{simulate_gp_1d}
\title{Simulate a 1-D Gaussian Process draw}
\usage{
simulate_gp_1d(x, eta = 1.5, rho = 2.5)
}
\arguments{
\item{x}{Numeric vector of 1-D input locations (e.g. time, dose, age).}

\item{eta}{Positive amplitude (marginal standard deviation) of the GP.}

\item{rho}{Positive length-scale of the GP, on the same scale as \code{x}.}
}
\value{
Numeric vector, same length as \code{x}: one draw of the latent GP
function \code{f} evaluated at \code{x}. No observation noise is added — this
is the noiseless truth surface, not the data.
}
\description{
Draws one realization of a mean-zero Gaussian Process with an
exponentiated quadratic (squared-exponential) kernel over a 1-D input,
via the exact (dense) Cholesky construction. Non-centered: an iid
standard normal vector \code{z} is transformed by the kernel's Cholesky
factor, \code{f = L \%*\% z}.
}
\examples{
x <- sort(runif(50, 0, 10))
f <- simulate_gp_1d(x, eta = 1, rho = 2)
plot(x, f, type = "l")

}
