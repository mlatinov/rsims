% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_state_space.R
\name{simulate_multistate}
\alias{simulate_multistate}
\title{Simulate correlated multivariate random-walk states}
\usage{
simulate_multistate(T, initial, Sigma, drift = NULL)
}
\arguments{
\item{T}{integer horizon}

\item{initial}{numeric vector of length k, starting values}

\item{Sigma}{k x k innovation covariance matrix}

\item{drift}{optional numeric vector of length k, constant drift added
at every step (default: no drift)}
}
\value{
\verb{T x k} numeric matrix, one column per state
}
\description{
Generalizes simulate_local_trend()-style coupling to k states whose
innovations are jointly Gaussian, e.g. level & slope moving together,
or several correlated regime states.
}
\details{
x_t = x_{t-1} + MVN(0, Sigma)
}
\examples{
# variances 1 and 0.25 (sds 1 and 0.5); covariance 0.3 => corr = 0.6
Sigma <- matrix(c(1, 0.3, 0.3, 0.25), nrow = 2)
xs <- simulate_multistate(T = 200, initial = c(100, 0.3), Sigma = Sigma)
}
