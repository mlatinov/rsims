% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_gps.R
\name{simulate_hsgp_2d}
\alias{simulate_hsgp_2d}
\title{Simulate a 2-D Hilbert Space GP (HSGP) approximation draw}
\usage{
simulate_hsgp_2d(
  longitude,
  latitude,
  M1 = 15,
  M2 = 15,
  rho = 2,
  alpha = 1,
  c = 1.5
)
}
\arguments{
\item{longitude}{Numeric vector of longitude (or x) coordinates.}

\item{latitude}{Numeric vector of latitude (or y) coordinates, same
length as \code{longitude}. Neither needs to be pre-centered — both are
centered internally.}

\item{M1, M2}{Number of basis functions along the longitude and latitude
axes respectively. Total basis size is \code{M1 * M2}; check sensitivity by
increasing both and confirming \code{f} stops changing appreciably.}

\item{rho}{Positive length-scale, shared isotropically across both axes.}

\item{alpha}{Positive amplitude (marginal standard deviation) of the
approximated GP.}

\item{c}{Boundary factor, same role as in simulate_hsgp_1d(), applied
independently on each axis.}
}
\value{
Numeric vector, one draw of the latent approximate GP surface
\code{f} evaluated at each coordinate pair. No observation noise is added
— add it yourself (e.g. \code{y <- f + rnorm(n, 0, sigma)}) so the same
noise-free \code{f} can be compared directly against simulate_gp_2d()
when validating the approximation.
}
\description{
Approximates a mean-zero GP over 2-D coordinates with an exponentiated
quadratic kernel using a tensor product of 1-D Laplacian-eigenfunction
bases, instead of the dense n x n kernel. Cost scales roughly
O(n * M1 * M2) rather than O(n^3), so this is the version to use once
simulate_gp_2d() becomes impractical (n in the thousands).
}
\examples{
lon <- runif(2000, 0, 10)
lat <- runif(2000, 0, 10)
f <- simulate_hsgp_2d(lon, lat, M1 = 15, M2 = 15, rho = 2, alpha = 1)

}
\seealso{
simulate_gp_2d() for the exact (dense) equivalent.
}
