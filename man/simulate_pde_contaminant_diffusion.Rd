% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_pde_contaminant_diffusion.R
\name{simulate_pde_contaminant_diffusion}
\alias{simulate_pde_contaminant_diffusion}
\title{Simulate Contaminant Diffusion Through Soil with Site-Level Diffusivity}
\usage{
simulate_pde_contaminant_diffusion(
  sites = 15,
  hours = 48,
  depth = 3,
  n_depth_points = 30,
  mean_initial_c = 2,
  sd_initial_c = 1,
  mean_diffusivity = 2,
  sd_sites_diffusivity = 1,
  beta_porosity = 0.1,
  safety_factor = 0.8,
  sensor_locations = c(0.3, 1.05, 1.95, 2.7),
  sensor_times = seq(0, hours, length.out = 12),
  sigma_obs = 0.1
)
}
\arguments{
\item{sites}{Integer. Number of independent soil sites.}

\item{hours}{Numeric. Total simulation duration.}

\item{depth}{Numeric. Maximum soil depth of the spatial domain.}

\item{n_depth_points}{Integer. Number of spatial grid points used to
discretize the soil profile.}

\item{mean_initial_c}{Numeric. Center location of the initial Gaussian
contaminant concentration profile.}

\item{sd_initial_c}{Numeric. Width parameter of the initial Gaussian
concentration profile.}

\item{mean_diffusivity}{Numeric. Mean diffusivity parameter before applying
site-level covariate effects.}

\item{sd_sites_diffusivity}{Numeric. Standard deviation of unexplained
site-level diffusivity variation.}

\item{beta_porosity}{Numeric. Effect of soil porosity on diffusivity.}

\item{safety_factor}{Numeric between 0 and 1. Multiplier applied to the
maximum stable numerical time step determined from the diffusion CFL
condition.}

\item{sensor_locations}{Numeric vector. Depth locations where observations
are collected.}

\item{sensor_times}{Numeric vector. Times where observations are recorded.}

\item{sigma_obs}{Numeric. Observation noise parameter.}
}
\value{
A list containing:

\describe{
\item{true_fields}{
Complete simulated PDE concentration fields for every site.
Contains the unobserved spatial process.
}

\item{observations}{
Simulated sensor measurements collected only at selected depths and times.
}

\item{parameters}{
Data frame containing site-level parameters including porosity and
diffusivity.
}
}
}
\description{
Simulates a hierarchical contaminant diffusion process across multiple soil
sites using a one-dimensional diffusion partial differential equation (PDE).

Each site has its own soil porosity value, which influences the site-specific
diffusion coefficient:

\deqn{
\log(D_j) =
\alpha_D +
\beta_D \times porosity_j +
\omega_j
}

where:
\itemize{
\item \code{D_j} is the diffusivity at site \code{j},
\item \code{porosity_j} is a site-level soil property,
\item \verb{\\omega_j} represents unexplained site-level variation.
}

The contaminant concentration field evolves according to a pure diffusion
equation:

\deqn{
\frac{\partial u_j(x,t)}{\partial t}
=
D_j
\frac{\partial^2 u_j(x,t)}{\partial x^2}
}

where \code{u_j(x,t)} represents contaminant concentration at depth \code{x} and time
\code{t} for site \code{j}.

The simulation separates:
\itemize{
\item site-level covariates,
\item PDE parameters,
\item spatial discretization,
\item temporal integration,
\item numerical stability control,
\item observation sampling.
}

The full concentration field is simulated internally, but measurements are
only collected at selected sensor depths and times, mimicking a real soil
monitoring experiment.

The observation layer follows:

\deqn{
y_{jkl} \sim LogNormal(
\log(u_j(x_l,t_k)),
\sigma_{obs}
)
}

where observations are noisy measurements from a limited number of sensors.
}
\details{
The simulation represents the causal structure:

\deqn{
porosity_j \rightarrow D_j \rightarrow u_j(x,t)
\rightarrow y_{jkl}
}

meaning soil structure influences contaminant diffusion through its effect
on the diffusion coefficient.

The numerical solver automatically adjusts the integration time step based
on the diffusion stability condition:

\deqn{
\frac{D\Delta t}{\Delta x^2}<0.5
}

ensuring stable diffusion simulations.
}
\examples{
\dontrun{
sim <- simulate_pde_contaminant_diffusion(
  sites = 15,
  hours = 72
)

head(sim$observations)

head(sim$parameters)
}

}
