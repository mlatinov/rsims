% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_latent.R
\name{simulate_latent_regression}
\alias{simulate_latent_regression}
\title{Simulate a latent variable predicted by observed covariates (MIMIC-style)}
\usage{
simulate_latent_regression(X, betas, beta0 = 0, sigma_zeta = 1)
}
\arguments{
\item{X}{Numeric matrix or data.frame of covariates (n x P). Columns
should already be centered/scaled as desired; this function does
not transform them.}

\item{betas}{Numeric vector of length P, one coefficient per column of X.}

\item{beta0}{Intercept.}

\item{sigma_zeta}{Residual SD of the structural disturbance zeta.}
}
\value{
list(
data  = list(eta = numeric vector, X = matrix),
truth = list(beta0 = ..., betas = ..., sigma_zeta = ..., n = ...)
)
}
\description{
Generative process:
eta_i  = beta0 + beta_1 x_1i + beta_2 x_2i + ... + zeta_i
zeta_i ~ Normal(0, sigma_zeta)
}
\details{
Example structure: Training, Age, Sleep -> Fitness (latent).
}
\examples{
X   <- data.frame(age = rnorm(300), sleep = rnorm(300))
sim <- simulate_latent_regression(X, betas = c(0.5, -0.3), beta0 = 0, sigma_zeta = 1)
}
