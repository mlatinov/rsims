% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_effects.R
\name{make_correlated_effects}
\alias{make_correlated_effects}
\title{Generate Correlated Random Effects}
\usage{
make_correlated_effects(n, means, sds, correlation_matrix)
}
\arguments{
\item{n}{Number of groups.}

\item{means}{Vector of population means for each random effect.}

\item{sds}{Vector of standard deviations for each random effect.}

\item{correlation_matrix}{Correlation matrix between effects.}
}
\value{
A matrix with one row per group and one column per random effect.
}
\description{
Generates multivariate normally distributed random effects with a user
specified covariance structure.

The generated effects follow:

\deqn{
\mathbf{u}_j \sim MVN(\boldsymbol{\mu},\Sigma)
}

where:

\deqn{
\Sigma = D R D
}

with \eqn{D} containing the standard deviations and \eqn{R} the
correlation matrix.

This is useful for simulating correlated varying intercepts and slopes.

Examples:

\deqn{
(\alpha_j,\beta_j)
\sim MVN
}

allowing hospitals, farms, clinics, or other groups to have correlated
baseline values and treatment effects.
}
\examples{

effects <- make_correlated_effects(
  n = 30,
  means = c(50, 0.5),
  sds = c(5, 0.2),
  correlation_matrix = matrix(
    c(1, 0.5,
      0.5, 1),
    nrow = 2
  )
)

}
\keyword{internal}
