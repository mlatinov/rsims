% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_splines.R
\name{simulate_group_p_spline}
\alias{simulate_group_p_spline}
\title{Simulate Group-Specific P-Splines (independent OR hierarchical)}
\usage{
simulate_group_p_spline(
  x,
  group,
  df = 8,
  tau = 0.2,
  sigma_group = 0.5,
  pool = TRUE,
  deviation = c("iid", "rw"),
  effect_size = 1
)
}
\arguments{
\item{x}{Predictor values.}

\item{group}{Group membership per observation.}

\item{df}{Number of basis functions.}

\item{tau}{Random-walk step size.}

\item{sigma_group}{SD of group deviations (used only when \code{pool = TRUE}).}

\item{pool}{Logical; \code{TRUE} for hierarchical (shared mean + deviations),
\code{FALSE} for independent per-group walks.}

\item{deviation}{\code{"iid"} or \code{"rw"} (used only when \code{pool = TRUE}).}

\item{effect_size}{Target SD applied as a common scale. \code{NULL} to leave raw.}
}
\value{
List with \code{basis}, \code{population_weights} (or \code{NULL} if unpooled),
\code{group_weights} (J x K), and \code{fitted}.
}
\description{
Generates a smooth nonlinear function per group over a shared B-spline basis.

\strong{Important:} the original version of this function drew a fresh,
independent random walk per group with \emph{no shared population curve} -- so
groups were NOT pooled and it was not hierarchical despite the name. This
version makes the behaviour explicit via \code{pool}:
\itemize{
\item \code{pool = FALSE} (the old behaviour): each group is an INDEPENDENT smooth
random walk. No population mean, no shrinkage.
\item \code{pool = TRUE} (default): a shared population random walk \code{w_bar} is
drawn first, and each group deviates around it by \code{sigma_group} (a genuine
hierarchical structure).
}

When \code{pool = TRUE}, group weights are \eqn{w_{jk} = \bar w_k + \delta_{jk}}
with \code{deviation} controlling whether the deviations are \code{"iid"} (rough) or
\code{"rw"} (smooth, anchored at 0), exactly as in
\code{simulate_hierarchical_spline_effect}.
}
\examples{
x <- runif(300, 0, 100); group <- rep(1:10, each = 30)
s <- simulate_group_p_spline(x, group, df = 8, pool = TRUE)
}
