% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_state_space.R
\name{simulate_state_regression}
\alias{simulate_state_regression}
\title{Add a regression component onto (optionally) an existing state}
\usage{
simulate_state_regression(state = NULL, X, betas)
}
\arguments{
\item{state}{optional numeric vector to add the regression term to
(e.g. a level or trend). If NULL, only X \%*\% betas is returned.}

\item{X}{a data.frame or matrix of predictors, one column per beta}

\item{betas}{numeric vector of coefficients, one per column of X}
}
\value{
numeric vector \code{eta}, same length as nrow(X)
}
\description{
eta = state + X \%*\% betas
}
\examples{
eta <- simulate_state_regression(
  state = level$state,
  X = data.frame(temp = rnorm(365), holiday = rbinom(365, 1, .05)),
  betas = c(-0.01, 0.25)
)
}
