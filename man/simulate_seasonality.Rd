% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_state_space.R
\name{simulate_seasonality}
\alias{simulate_seasonality}
\title{Recycle a seasonal effects pattern out to an arbitrary horizon}
\usage{
simulate_seasonality(period, effects, T = NULL, center = TRUE)
}
\arguments{
\item{period}{length of one seasonal cycle (e.g. 7 for day-of-week)}

\item{effects}{numeric vector of length \code{period} with the effect for
each phase of the cycle}

\item{T}{horizon to recycle out to. Defaults to \code{period} (one full
cycle) if not supplied.}

\item{center}{if TRUE, subtract mean(effects) so the pattern is centered
before recycling}
}
\value{
numeric vector of length T (named \code{season} when used in \code{$}
contexts downstream)
}
\description{
Wraps the \code{rep(effects, length.out = T)} pattern you'd otherwise retype
in every script, plus optional centering so the seasonal component sums
to (approximately) zero and doesn't fight with the level/intercept.
}
\examples{
weekly <- simulate_seasonality(period = 7,
                                effects = c(-.1, -.2, -.1, 0, .15, .25, .10),
                                T = 365, center = TRUE)
}
