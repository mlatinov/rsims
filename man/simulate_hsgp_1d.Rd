% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_gps.R
\name{simulate_hsgp_1d}
\alias{simulate_hsgp_1d}
\title{Simulate a 1-D Hilbert Space GP (HSGP) approximation draw}
\usage{
simulate_hsgp_1d(x, M = 15, rho = 2, alpha = 1, c = 1.5)
}
\arguments{
\item{x}{Numeric vector of 1-D input locations. Does not need to be
pre-centered — it is centered internally so the basis domain
\verb{-L, L} is well placed around the data.}

\item{M}{Number of basis functions (eigenfunctions). Larger \code{M} gives a
more accurate approximation at higher cost; check sensitivity by
increasing \code{M} and confirming \code{f} stops changing appreciably.}

\item{rho}{Positive length-scale of the approximated GP.}

\item{alpha}{Positive amplitude (marginal standard deviation) of the
approximated GP.}

\item{c}{Boundary factor: how far the basis domain extends past the
range of centered \code{x}, as a multiple of \code{max(abs(x - mean(x)))}.
Typical values are 1.2--2; too small biases the fit near the edges of
the data.}
}
\value{
Numeric vector, same length as \code{x}: one draw of the latent
approximate GP surface \code{f}. No observation noise is added.
}
\description{
Approximates a mean-zero GP with an exponentiated quadratic kernel using
a fixed Laplacian-eigenfunction basis on a bounded domain, instead of
the dense n x n kernel. Cost scales O(n * M) rather than O(n^3), so this
is the version to use once simulate_gp_1d() becomes impractical.
}
\examples{
x <- sort(runif(500, 0, 10))
f <- simulate_hsgp_1d(x, M = 20, rho = 2, alpha = 1)

}
\seealso{
simulate_gp_1d() for the exact (dense) equivalent, useful for
validating the approximation on the same synthetic input.
}
