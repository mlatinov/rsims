% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_state_space.R
\name{simulate_hierarchical_states}
\alias{simulate_hierarchical_states}
\title{Simulate independent local-level states for many groups (e.g. stores)}
\usage{
simulate_hierarchical_states(
  groups,
  T,
  initial_mean = 0,
  sigma_between = 1,
  sigma_state = 1
)
}
\arguments{
\item{groups}{integer number of groups (e.g. 30 stores)}

\item{T}{integer horizon}

\item{initial_mean}{grand mean of the group-level starting points}

\item{sigma_between}{sd of group-level starting points around
initial_mean}

\item{sigma_state}{innovation sd of each group's local level}
}
\value{
a \verb{groups x T} numeric matrix, \code{state[group, time]}
}
\description{
Each group j gets its own random-walk state, with group-level starting
points drawn from N(initial_mean, sigma_between^2) and within-group
innovation variance sigma_state^2.
}
\details{
state j, t = mu_j0 + cumsum(eps_{j,t}),  eps_{j,t} ~ N(0, sigma_state^2)
mu_j0 ~ N(initial_mean, sigma_between^2)
}
\examples{
states <- simulate_hierarchical_states(groups = 30, T = 365,
                                        initial_mean = log(50),
                                        sigma_between = .2,
                                        sigma_state = .01)
}
