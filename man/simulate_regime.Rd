% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_state_space.R
\name{simulate_regime}
\alias{simulate_regime}
\title{Simulate a piecewise-constant regime / structural-break series}
\usage{
simulate_regime(T, block_length, effects, center = FALSE)
}
\arguments{
\item{T}{integer horizon}

\item{block_length}{length of each regime/block in time units}

\item{effects}{numeric vector, one value per block. Length should equal
\code{T / block_length}; if there are fewer effects than blocks, they
recycle (e.g. ABAB...); if there are more, the extras are unused.}

\item{center}{if TRUE, subtract mean(effects) so the pattern is centered
before expanding}
}
\value{
numeric vector of length T
}
\description{
The mirror image of simulate_seasonality(): instead of tiling a short
repeating pattern many times across T (fine-grained, \code{rep(effects, length.out = T)}), this HOLDS each value constant for \code{block_length}
periods before switching to the next one (coarse-grained, \code{rep(effects, each = block_length, length.out = T)}).
}
\details{
Use simulate_seasonality() when every unit \emph{within} one cycle differs
(e.g. Monday vs Tuesday vs Wednesday, repeated every week).
Use simulate_regime() when a single value holds for a whole block, and
blocks change less often than every observation (e.g. a reservoir
sitting at one operating level for 50 days, then switching).

Number of distinct effects needed = T / block_length (one per block).
If that isn't a whole number, the final block is truncated and a
warning is issued so it never fails silently.
}
\examples{
# 4 regimes of 50 days each across a 200-day horizon
regime <- simulate_regime(T = 200, block_length = 50,
                           effects = c(0, -4, 5, 3))
}
