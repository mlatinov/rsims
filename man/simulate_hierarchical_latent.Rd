% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_latent.R
\name{simulate_hierarchical_latent}
\alias{simulate_hierarchical_latent}
\title{Simulate a hierarchical (multilevel) latent variable}
\usage{
simulate_hierarchical_latent(
  n_groups,
  n_per_group,
  sigma_mu = 1,
  sigma = 1,
  ncp = TRUE
)
}
\arguments{
\item{n_groups}{Number of groups J.}

\item{n_per_group}{Single integer (equal group sizes) or an integer
vector of length n_groups.}

\item{sigma_mu}{SD of the group means (spread across groups).}

\item{sigma}{Residual SD of units within a group (wobble).}

\item{ncp}{Logical; use the non-centered parameterization (default TRUE).}
}
\value{
list(
data  = list(eta = numeric vector, group_id = integer vector),
truth = list(mu_j = ..., sigma_mu = ..., sigma = ..., n_groups = ..., n_per_group = ...)
)
}
\description{
Generative process:
mu_j   ~ Normal(0, sigma_mu)          j = 1..J   (group means)
eta_i  ~ Normal(mu_J i, sigma)       i = 1..n   (unit latent)
}
\details{
Non-centered parameterization (default):
mu_j   = sigma_mu * z_mu_j,   z_mu_j ~ Normal(0, 1)
eta_i  = mu_J i + sigma * z_i,  z_i ~ Normal(0, 1)

Example structure: Hospital -> Patient latent quality.
}
\examples{
sim <- simulate_hierarchical_latent(n_groups = 20, n_per_group = 15,
                                     sigma_mu = 1, sigma = 0.5)
}
