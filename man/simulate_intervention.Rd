% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_state_space.R
\name{simulate_intervention}
\alias{simulate_intervention}
\title{Simulate an intervention / event indicator series}
\usage{
simulate_intervention(
  T,
  time,
  type = c("step", "pulse", "ramp", "decay"),
  duration = NULL,
  decay_rate = 0.8,
  magnitude = 1
)
}
\arguments{
\item{T}{integer horizon}

\item{time}{the index at which the intervention starts (1-based)}

\item{type}{one of "step", "pulse", "ramp", "decay"}

\item{duration}{for "pulse": how many periods it stays on (default 1).
unused for "ramp" (which always increments 1, 2, 3, ... to the end
of the series) and "decay".}

\item{decay_rate}{for "decay": multiplicative decay per period (0 < r < 1)}

\item{magnitude}{scalar multiplier applied to the whole series (default 1)}
}
\value{
numeric vector of length T
}
\description{
Replaces the one-off \code{as.numeric(time >= t0)} (and friends) that shows
up in nearly every causal-impact-style simulation.
}
\examples{
step  <- simulate_intervention(T = 14, time = 8, type = "step")
pulse <- simulate_intervention(T = 14, time = 6, type = "pulse", duration = 1)
ramp  <- simulate_intervention(T = 14, time = 6, type = "ramp")
decay <- simulate_intervention(T = 14, time = 6, type = "decay", decay_rate = 0.8)
}
