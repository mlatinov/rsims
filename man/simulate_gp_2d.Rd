% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_gps.R
\name{simulate_gp_2d}
\alias{simulate_gp_2d}
\title{Simulate a 2-D Gaussian Process draw over coordinates}
\usage{
simulate_gp_2d(longitude, latitude, eta = 1.5, rho = 2.5)
}
\arguments{
\item{longitude}{Numeric vector of longitude (or x) coordinates.}

\item{latitude}{Numeric vector of latitude (or y) coordinates, same
length as \code{longitude}.}

\item{eta}{Positive amplitude (marginal standard deviation) of the GP.}

\item{rho}{Positive length-scale of the GP, on the same scale as the
coordinates.}
}
\value{
Numeric vector, one draw of the latent GP surface \code{f} evaluated
at each coordinate pair. No observation noise is added.
}
\description{
Draws one realization of a mean-zero Gaussian Process with an
exponentiated quadratic kernel over a 2-D input (e.g. longitude/
latitude), via the exact (dense) Cholesky construction. Same pattern as
simulate_gp_1d(), with Euclidean distance in place of absolute
difference.
}
\details{
Dense (exact) construction: cost scales as O(n^3) via the
Cholesky factorization, so this is only practical up to roughly a few
thousand points. For larger \code{n}, use simulate_hsgp_2d().
}
\examples{
lon <- runif(80, 0, 10)
lat <- runif(80, 0, 10)
f <- simulate_gp_2d(lon, lat, eta = 1.5, rho = 2.5)

}
