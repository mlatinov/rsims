% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_sem.R
\name{simulate_outcome}
\alias{simulate_outcome}
\title{Simulate an outcome from a latent (or observed) predictor}
\usage{
simulate_outcome(
  eta,
  family = c("gaussian", "bernoulli", "poisson"),
  beta0,
  beta1,
  sigma = NULL,
  X = NULL,
  betas_x = NULL
)
}
\arguments{
\item{eta}{Numeric vector, the latent (or observed) predictor.}

\item{family}{One of "gaussian", "bernoulli", "poisson".}

\item{beta0}{Intercept.}

\item{beta1}{Coefficient on eta.}

\item{sigma}{Residual SD, required only when family == "gaussian".}

\item{X}{Optional additional covariate matrix (n x P) entering the
linear predictor additively (for outcome models with more than
one predictor).}

\item{betas_x}{Optional numeric vector of length P, coefficients for X.}
}
\value{
list(
data  = list(Y = numeric/integer vector, eta = eta, X = X),
truth = list(beta0 = ..., beta1 = ..., sigma = ..., betas_x = ...,
family = ..., n = ...)
)
}
\description{
Generative process
mu_i = beta0 + beta1 * eta_i
gaussian:  Y_i ~ Normal(mu_i, sigma)
bernoulli: Y_i ~ Bernoulli(logistic(mu_i))
poisson:   Y_i ~ Poisson(exp(mu_i))
}
\details{
The latent enters through the linear predictor and then a
family-specific inverse link and sampling distribution, e.g.
Engagement -> Exam score, or Disease severity -> Mortality.
}
\examples{
engagement <- simulate_latent(n = 400)$data$eta
sim <- simulate_outcome(engagement, family = "gaussian",
                         beta0 = 50, beta1 = 5, sigma = 8)

severity <- simulate_latent(n = 400)$data$eta
sim2 <- simulate_outcome(severity, family = "bernoulli",
                          beta0 = -2, beta1 = 1.5)
}
